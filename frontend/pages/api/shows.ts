import type { NextApiRequest, NextApiResponse } from "next";
import type { ShowsSearch } from "../../api/types";
import { BASE_URL } from "../../config";

export default async function handler(req: NextApiRequest, res: NextApiResponse<ShowsSearch>) {
  // const {}
}